// Tailwind 4 Play CDN uses <style type="text/tailwindcss"> @theme { ... } </style> blocks in each HTML file.
// This legacy Tailwind 3 config file is intentionally left without runtime configuration.
